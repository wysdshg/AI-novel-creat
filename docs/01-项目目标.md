# 01 · 项目目标与预期效果

> 本文是项目的**唯一目标定义**。任何需求争议以本文为准。  
> 最后更新：2026-09-10（复核进度表 / §5.1 检索已升级 / §7 差距表校正）



---

## 1. 这是什么

一个**本机单人使用的长篇小说写作辅助工具**（B/S 架构，浏览器访问）。

不是产品，不是 SaaS，不给第二个人用。所有设计决策以"作者本人写得更快、更稳、不崩"为唯一判据。

---

## 2. 使用者与场景

- **使用者**：一个人（作者本人）
- **硬件**：本机，RTX 4060 Laptop 8GB，本地 Ollama（qwen3.5:4b）+ 云端模型（**ModelScope Qwen/Qwen3.8-Flash-Next【当前默认，2026-09-09 换】** / NVIDIA GLM-5.2 / SiliconFlow Qwen3-8B）
- **检索模型选型（2026-09-09 定，供检索升级用）**：Embedding = 硅基流动 **BAAI/bge-m3**；重排 = 硅基流动 **BAAI/bge-reranker-v2-m3**。均为免费档。

### 题材：**题材无关**（重要）

工具必须能适配**多种题材**，不是某一本书的定制器。
- 架空王朝（宋式小盛世基调，文武平等）探案/官场向 —— **仅为当前测试题材**，不是目标
- 凡是与"某一种题材"强绑定的设计（写死的模板、固定境界体系、特定官职链），都是**待改造项**
- 题材差异应通过「设定库 + 参考文档 + SKILL」承载，而不是硬编码进代码

### 当前进度（2026-09-10 实测）

| 项 | 数量 |
|---|---|
| 作品 `projects` | **0** |
| 卷 / 篇 / 章 / 章记忆 | 0 / 0 / 0 / 0 |
| 全局资料池 `reference_docs` | **18** 份 |
| 全局设定 `settings` | **21** 条 |
| 全局向量块 `vector_chunks` | **133** |
| 全局对话 `discussion_messages` | 30 |
| 模型配置 `model_configs` | 5 |

> **历史数据说明（重要，防止误判产量）**
>
> - 9-09 时库内有 2 部作品（`AA`、`古代哪来的黑社会`），**现已不在库中**；
>   9-09 20:09 的备份里仍有，如需找回见下方备份列表。
> - 当时另有 **62%~91% 的孤儿残留**（`volumes` 20 条里 18 条、`articles` 22 条里 20 条、
>   `chapter_memories` 33 条里 29 条等），全部来自 **2026-08-13 修复级联删除之前**的 21 部已删作品。
>   已验证**当时的删除逻辑是干净的**（新建→塞数据→删除，残留 0），孤儿是历史遗留而非新 bug。
> - 孤儿已于 **2026-09-10 清空**（102 行；流程、校验与 `__global__` 豁免见 03 §3.1），清理后上述全局数据未受影响。
> - **结论：看产量一律以本表为准**，不要被历史残留或备份里的旧数字误导。
>
> **DB 备份（`C:\Users\w3013\.ai_novel\data\`）**：
> | 文件 | 时点 | 作品 |
> |---|---|---|
> | `novel_agent.db.bak.20260909_200949` | 9-09 20:09 | **2 部（AA + 古代哪来的黑社会）** |
> | `novel_agent.db.bak.20260910_135431` | 9-10 13:54 | 1 部（e2e 测试作品） |
> | `novel_agent.db.bak.20260910_135942` | 9-10 13:59 | 1 部（同上，孤儿清理前） |

---

## 3. 核心命题：AI 该接管什么

**判据：AI 只接管"人做不到"的事，不接管"人不愿意做的苦力"。**

| 交给 AI（人做不到）                     | 留给作者（人是强项）     |
| ------------------------------- | -------------- |
| 跨几十章的**一致性**（人设不崩、设定不自相矛盾）      | 故事结构与大纲        |
| 跨几十章的**伏笔追踪**（埋了没、收了没、哪条烂尾）     | 文风与审美判断        |
| 长篇**上下文压缩**（写到第 80 章还记得前面发生了什么） | 情节创意与走向选择      |
| **设定校验**（写的内容是否违反已定规则）          | 是否采纳 AI 的每一个建议 |
| 写后**自动沉淀**（记忆/摘要/大事记/新实体）       | 最终成稿的所有文字      |

### ⚠️ 关于套路模板（2026-09-09 修正）

**上一版写"套路模板价值最低、列为删除对象"，这个判断是错的，已推翻。**

- **流水线小说（网文量产）恰恰最需要套路模板**——这是它的生产工具，不是可选项。
- 目前的 7 条写死模板（秘境夺宝/宗门大比/穿越重生…）之所以看起来没价值，
  **不是因为模板没用，而是因为它是硬编码的、题材绑定的、且不能编辑**——与上文"题材无关"原则直接冲突。
- **正确方向**：模板应做成**可编辑、可新增、题材中立的结构化数据**（节奏骨架 / 情节节点 / 冲突类型 / 爽点分布），
  而不是一份写死的列表。
- **状态：实现方式待考究**（见 03）。当前不做删除，先冻结。

**修正后的判据**：不是"AI 别碰创意"，而是"**AI 提供结构与素材，作者掌握取舍**"。

---

## 4. 预期效果（成功标准）

一条主线闭环，跑通即算成功：

```
作者写/聊剧情 → AI 生成章节（流式，可中断）
              → 写后自动沉淀（记忆/摘要/大事记/新实体/伏笔）
              → 下一章自动带上前情、设定、伏笔
              → 作者随时查"埋了哪些伏笔没收""前面发生过什么"
```

**可验收的四条硬指标**：

1. **不崩**：写到第 50 章时，AI 仍准确知道前 49 章发生了什么（靠记忆层，不靠硬塞全文）
2. **不飘**：生成的正文不违反已录入的设定（靠设定校验，不是靠祈祷）
3. **不漏**：埋下的伏笔能被追踪到回收或标记为烂尾
4. **改得动**：改任何一处代码后，能在 5 分钟内确认没改坏（靠测试 + 日志 + 变更记录）

---

## 5. 明确不做（防止范围漂移）

| 不做            | 原因                                    |
| ------------- | ------------------------------------- |
| 多用户 / 登录 / 权限 | 单人本机工具                                |
| 云端部署 / 容器化    | 同上                                    |
| 移动端适配         | 作者只在桌前写作                              |
| 文风 LoRA 微调    | 成本远超收益，提示词工程已够用                       |
| ~~向量检索 / RAG~~ | ~~已撤销~~ → **不仅撤销限制，且已实施完成**，见 §5.1 |
| 协同编辑 / 版本分支   | 单人写作不需要                               |
| 100% 测试覆盖率    | 只测容易崩的地方（见 06）                        |

### 5.1 检索能力：已升级（2026-09-10 完成）

**原状**：目录 + 关键词（2-gram）+ 按需加载（`LOAD_REFS` / `LOAD_SETTING`），能用但受字面匹配限制。

**升级方向（下表各项已全部实施 —— Phase R A 线 A1~A6，2026-09-10 真机验收）**：

| 项      | 说明                                             |
| ------ | ---------------------------------------------- |
| Embedding | **硅基流动 BAAI/bge-m3**（免费档，2026-09-09 选定）    |
| 重排 Rerank | **硅基流动 BAAI/bge-reranker-v2-m3**（免费档，2026-09-09 选定） |
| 存储     | 计划迁移到**向量库**                                    |
| 高级检索   | 部分场景（人物关系 / 事件因果 / 势力博弈）**GraphRAG 更合适**       |
| 检索策略   | **Hybrid 混合检索**（向量 + 关键词 + 图谱）                  |

> **状态：已完成 ✅（2026-09-10）**。存储用 **sqlite-vec**（`vec_index` 虚拟表，带 `project_id`/`source_type`
> 辅助列做 KNN 内过滤；扩展不可用时自动回退纯 Python 余弦）；检索为 **Hybrid RRF 融合**（k=60，只用名次不用原始分）
> + **bge-reranker-v2-m3 重排**（取 top_k×3 候选 → 逐对打分 → top_k）+ **实体图一跳扩展**（刻意不递归）。
> key 已写入 `app_configs retrieval.siliconflow_key`，全局池 18 份 → **133 向量块**已激活。
> 开关 `retrieval.vector_index` / `retrieval.graph_expand`（均默认开；**无 key 时全链静默跳过，行为与升级前完全一致**）。
> 详见 02 已实现清单、03 Phase R、05 §6.5。

---

### 5.2 工作流：方案待定（2026-09-09）

**当前实现对效果不满意**，工作流"彻底该怎么做"尚未想清楚，暂列为**待定**。

已知的问题（不是 bug，是设计层面没想透）：

- 工作流生成的章节**不触发写后摄取**，与主链路行为不一致（双轨制）
- 3 个工作流、7 次运行——用得少，说明它没解决真实痛点
- 画布编辑器（771 行）+ DAG 引擎（800 行）投入不小，但产出价值不明

**在想清楚之前：不投入、不删除、不扩展。** 见 03 §3 战略待拍板。

---

## 6. 技术约束（已定，不讨论）

- 前端 Vue3 + Element Plus + Vite + Pinia
- 后端 Python 3.12 + FastAPI + SQLAlchemy 2.0
- 数据库 SQLite 单文件（`C:\Users\w3013\.ai_novel\data\novel_agent.db`），无迁移工具
- Python 环境固定 `E:\AI小说创作\.venv`（**不是** WorkBuddy 受管环境）
- 后端端口 8000，前端 5173

---

## 7. 与"行业完整 Agent 流程"的差距（诚实记录）

按 12 环对照，本项目现状：

| 环节          | 状态                                                    |
| ----------- | ----------------------------------------------------- |
| 需求建模        | ✅ 本文                                                  |
| 知识 / 检索     | ✅ **已升级**：Hybrid 检索（关键词 + 向量 RRF 融合）+ rerank 重排 + 实体图一跳扩展，已激活并真机验收（§5.1） |
| 工具调用        | 🟡 **现状**：`LOAD_REFS`/`LOAD_SETTING` 自研行内文本协议（无 schema 校验、无重试、无结果回灌）。**扩展规划见 03 Phase 6** |
| 编排          | ⚠️ DAG 引擎真实执行，但**效果不满意、方案待定**（§5.2）                  |
| 记忆          | ✅ **最强项**：章级 + 阶段 + 大事记 + 待确认实体                       |
| 生成          | ✅ SSE 流式 + 上下文预算裁剪                                    |
| **评估 eval** | 🟡 **人工打分已上线**（Phase 4.1：5 分制 + 同章版本对比）；自动评估未做       |
| **观测**      | ✅ **已打通（2026-09-10）**：用量计量表 + `trace_id` 贯通「生成 ↔ 日志 ↔ 用量明细」。目前覆盖**章节生成链路** |
| **反馈闭环**    | 🟡 **章节改动已回流**（`feedback_records`，2026-09-10）；走向采纳/拒绝未做     |
| 安全护栏        | ❌ 无注入防护                                               |
| **工程基建**    | ✅ **160 单测 + 后端日志落盘（含崩溃取证）+ CHANGELOG**（Phase 0/3 完成）；仅 SQLite 无迁移工具（`init_db` 自动加列）                             |
| 成本治理        | 🟡 **token 计量已上线**（区分厂商真实回流 / 字符估算，覆盖章节链路）；金额换算未做 |

**结论（2026-09-10 复核）**：功能层（Phase 1-2）与**工程质量（Phase 0/3）已达标** —— 测试、日志、变更记录三件套齐全，
检索也从"待优化"升级为已实施（§5.1）。
**结论更新（2026-09-10 晚，Phase 4.1~4.3 落地后）**：「三环」中**观测与反馈的骨架已通** ——
用量计量（区分真实/估算）、`trace_id` 贯通、章节改动自动回流均已上线；人工打分（Phase 4.1）也已就位。
**仍差的**：自动评估、商讨/摄取链路的用量覆盖、走向采纳/拒绝反馈（见 03 Phase 4「仍未做」）。
次要缺口：工具调用无 schema 与回灌（规划见 03 Phase 6）、安全护栏（无注入防护）。
