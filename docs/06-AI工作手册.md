# 06 · AI 工作手册（**每次开工必读**）

> 这是唯一一份 AI 无条件必读的文档。它同时是索引：读完本文就知道该不该读别的。  
> 违反任何一条 🔴 铁律的改动都是不可接受的。

---

## 1. 文档索引：什么时候读哪份

| 场景                                | 读哪份                           |
| --------------------------------- | ----------------------------- |
| **任何任务开始前**                       | 本文（06）                        |
| 任务分发 / 多 agent 协同（何时投递、怎么投） | `07-任务分发手册.md`                 |
| 问"这项目要干嘛 / 该不该做这个功能"              | `01-项目目标.md`                  |
| 问"某某功能实现了吗 / 能用吗"                 | **`02-已实现清单.md`**（唯一可信来源，实测版） |
| 问"接下来做什么 / 某模块留还是删"               | `03-规划与进行中.md`                |
| **改提示词 / 解码参数 / 模型适配器 / 流式代码 之前** | **`04-踩坑档案.md`**（必读，血的教训）     |
| 问"接口怎么调 / 架构什么样 / 环境变量"           | `05-架构与接口.md`                 |

### 🔴 禁止读取

- `docs/_archive/` 下所有文件 —— **已过时/有错/会误导**。除非用户在指令中明确点名要查某个历史文件。
- `.workbuddy/memory/` 下的历史日志（2026-08-06 ~ 08-14）—— 内容已提炼进 04，原文 294KB 会污染上下文。
- 任何 2026-09-09 之前生成的"项目说明/接口规范/体检报告"。

> 这些文件**没有删除**，全部保留在 `docs/_archive/`，需要考古时可以查，但**常规任务一律不读**。

---

## 2. 🔴 铁律（违反即回退）

### 2.1 准确性铁律

1. **不许凭读代码或读旧文档下结论。** 判断模块是否可用，以 `02-已实现清单.md` 为准；未实测的一律标 🟡，不许标 ✅。
2. **更新 02 必须实测**：改完代码要真发请求验证，然后把结果回填 02（含验证方式与日期）。
3. **不许说"应该可以""大概没问题"**。不确定就标 ⚠️ 存疑并写清疑点。
4. 发现 02/04/05 与代码不符 → **先改文档再改代码**，并把更正记录进 `CHANGELOG.md`。

### 2.2 代码铁律

1. **流式生成器内严禁访问请求级 DB session / ORM 对象。** 必须自建 `gen_db = _db.SessionLocal()`（模块引用，非直接 import），`finally` 关闭。见 04-C1。违反会导致 `DetachedInstanceError` → 流中断。
2. **SSE 所有业务事件必须在 `event: done` 之前发完。** 见 04-C2。
3. **禁止用 `print` 调试**——一律 `logging`。禁止打印用户正文/请求体/对话历史（**Phase 1.1 已全量清理完毕**：2026-09-10 清理 5 处，含 gateway 写盘泄漏；改动后仍须守此律）。
4. **禁止把异常文案写入章节正文**（`chapter.py` 曾把 `[模型调用失败…]` 落库）。
5. **前端 `fetch` 必须校验 `resp.ok`**（`chapter.js` 曾漏，导致界面永久卡在"生成中"）。
6. **有 alias 的 Pydantic schema，`model_dump` 必须带 `by_alias=True`**。见 04-C4。

### 2.3 流程铁律

1. **一次只改一个模块。** 改完必须验证 + 更新 02/03 + 记 CHANGELOG，再动下一个。
2. **动业务代码前先确认 Phase 0 是否已完成**（测试骨架 + logging + CHANGELOG）。未完成则先补。
3. **改后端后必须提醒用户手动重启**（`E:\` 共享盘 uvicorn reload 不生效）。见 04-A1。
4. 沙箱内起后端一律 `DEV_RELOAD=0`（否则产生关不掉的 8000 孤儿进程）。见 04-A2。
5. Python 固定用 `E:\AI小说创作\.venv\Scripts\python.exe`，**不是** WorkBuddy 受管环境。

---

## 3. 改这几类东西前的强制检查

| 你要改…                       | 先读          | 重点                                                      |
| -------------------------- | ----------- | ------------------------------------------------------- |
| **提示词 / 系统提示 / SKILL**     | 04-B5、B6    | 约束叠加会反向致病态输出；负面措辞会 priming                              |
| **解码参数（温度/惩罚/max_tokens）** | 04-B1、B8    | Qwen3.5+ModelScope 必须禁用重复惩罚；短测试通过是假象                    |
| **模型适配器 / 新增厂商**           | 04-B2、B3、B7 | 思考字段厂商白名单；Ollama 用 `num_predict` 且 `num_ctx` 会静默丢 token |
| **流式 / SSE 相关**            | 04-C1、C2    | DetachedInstanceError + 事件时序                            |
| **工作流引擎**                  | 04-D5       | end 节点虚拟入度固定为 1                                         |
| **前端弹窗（有流程状态的）**           | 04-C6       | `el-dialog` 常驻挂载不销毁状态，必须 watch 复位                       |
| **数据库表结构**                 | 04-A5       | SQLite 无迁移工具，只能加列                                       |

> **⚠️ 要动「工具调用 / function calling / 检索选择策略」** → 先读 **03 Phase 6**（完整规划：P0~P3 优先级、工程配套、前提条件与推进顺序）
> 与 **05 §11**（现有 4 种交互模式 + `reference_selector.py` 的 A/B 策略抽象层）。
> **两条硬约束**：① 适配器当前只返回纯文本、**不暴露 `tool_calls`**，策略 B 仍是占位；
> ② **本地 4b 连 marker 协议都不稳**（`chapter.py:254` 有当时的取舍记录），必须保留 marker 降级，别硬上原生工具调用。

---

## 4. 常见误判（别再犯）

| ❌ 错误判断              | ✅ 正确                               |
| ------------------- | ---------------------------------- |
| "数据库 0 行 = 这模块是死的"  | 0 行可能是未使用/未触发/走默认值。必须 grep ORM 读写点 |
| "旧 README 标 ✅ 就是能用" | 旧 README 把桩模块标成 ✅，已被证伪多次。**只信 02** |
| "数据库里 8 章 = 写了 8 章" | 行数可能含已删作品留下的孤儿。先做 `project_id` 差集再引用任何行数（04-D6）。**2026-09-10 已清 102 行，但方法照旧**，且新表要记得登记进级联删除 |
| "短生成测试通过就没问题"       | 中后段退化必须长输出才复现（04-B1）               |
| "某功能价值低 → 可以删" | **先确认它服务哪种创作模式**。精品单本 vs 流水线量产，同一个功能价值可以完全相反（套路模板就是反例，见 01） |
| "两个模型同时坏了 = 模型问题"   | 先怀疑公共链路：超时/网关/后端未重启（04-B9）         |
| "约束加得越多 AI 越听话"     | 禁忌类必须互斥，叠加会逼出病态输出（04-B5）           |

---

## 5. 提交前的自检清单

- [ ] 改动是否只涉及一个模块？
- [ ] 是否实测过（发过真实请求 / 跑过构建）？
- [ ] `02-已实现清单.md` 是否已更新（含验证方式与日期）？
- [ ] `03-规划与进行中.md` 的进度是否已勾？
- [ ] `CHANGELOG.md` 是否记了一行？
- [ ] 是否引入了新的 `print`？是否打印了用户数据？
- [ ] 若改了后端，是否已提醒用户重启？

---

## 6. 环境速查

| 项       | 值                                                                        |
| ------- | ------------------------------------------------------------------------ |
| Python  | `E:\AI小说创作\.venv\Scripts\python.exe`（Py 3.12.10）                         |
| 后端      | `cd E:/AI小说创作/backend && <py> dev.py` → `http://127.0.0.1:8000/docs`     |
| 前端      | `cd E:/AI小说创作/frontend && npm run dev` → `http://localhost:5173`         |
| 数据库     | `C:\Users\w3013\.ai_novel\data\novel_agent.db`                           |
| curl 本机 | 一律加 `--noproxy '*'`（否则被系统代理劫持）                                           |
| 删文件     | 用 PowerShell `Remove-Item -LiteralPath '绝对路径' -Force`（bash `rm` 会被安全钩子拦） |
